/**
 * Interface: FileHtmlInterface
 * Handles the dom elements for both document | stamp
 */
export interface FileHtmlInterface {
  getElementLayout(index: number, file: any, formData: any);
}

// export { FileHtmlInterface };
